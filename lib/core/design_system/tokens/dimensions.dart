import 'package:flutter/material.dart';

// Spacing
const sp2  = 2.0;
const sp4  = 4.0;
const sp6  = 6.0;
const sp8  = 8.0;
const sp10 = 10.0;
const sp12 = 12.0;
const sp16 = 16.0;
const sp20 = 20.0;
const sp24 = 24.0;
const sp32 = 32.0;
const sp40 = 40.0;
const sp48 = 48.0;
const sp64 = 64.0;
const sp80 = 80.0;

// Border radius
final radius4   = BorderRadius.circular(4);
final radius8   = BorderRadius.circular(8);
final radius12  = BorderRadius.circular(12);
final radius16  = BorderRadius.circular(16);
final radius24  = BorderRadius.circular(24);
final radiusPill = BorderRadius.circular(100);

// Elevation shadows
const shadowRaised = [
  BoxShadow(color: Color(0x0A000000), blurRadius: 8, offset: Offset(0, 2)),
];
const shadowFloating = [
  BoxShadow(color: Color(0x14000000), blurRadius: 24, offset: Offset(0, 8)),
];
